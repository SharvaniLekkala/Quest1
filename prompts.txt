1. Prepare a structure for the project that basically takes in input of a publicly available URL of a long video and outputs exact frame, text, timestamp, frame number and image at that moment Input: video <URL> target "<dialogue text>" Output: ONE result -> {timestamp, frame_number, extracted_text, frame_image} as a json file generated I want the code to be modular, scalable easily I want a workign front end and backend so that i dont want to have cmd inputs giev me the current inside architecture proper and then the project structure and along with a single cmd to create the full folders and the subfiles in it, then we shall proceed further
2. Give me a single line command to create all the files in the directory at once
3. lets go ahead with the backend development first, give me the options and trade offs and challenges in solvign this problem in different ways using different concepts of extracting using whisper/ AI agents / ML models / pre trained models/ huggingface models? I wnat the full product to be lightweight and fast as well.
4. In different vidoes there might be noise in the audio as well i need to tackle all of that too, noice in the audio. Similar to these situations that are ambiguios are there any situations that i might be facing during this flow?
5. now that you had listed the ambiguities too, i want my project to handle all of them and be robust give the command to execute to add any new files to add in the current directory based on the new ambiguity testing etc
6. i wnat to start the build of code now lets begin with backend project i want to go step by step and debug if in case of any errors. go code by code and give why we are using those features and whats special about the code
